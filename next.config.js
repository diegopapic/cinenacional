/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Agrega aquí los dominios de imágenes que uses
  },
}

module.exports = nextConfig